# wechat-agent-sdk 由 vendor/wechat_agent_sdk 提供（0.2.1 全量快照，10 组补丁预打，
#   清单见 patches/apply_patches.py；上游版本号与补丁记录见 vendor/wechat_agent_sdk/_VENDOR.md）：
#   pip install -e ./vendor/wechat_agent_sdk
# 下方为 SDK 运行时依赖（版本锁死，与 PyPI 版 SDK 一致）
agent-client-protocol==0.12.0
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.1
cryptography==50.0.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
lunar_python==1.4.8
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
typing-inspection==0.4.4
typing_extensions==4.16.0

# 测试（dev 依赖）
pytest==9.1.1

# 配置化（bridge/config.py 读 YAML，用户段可配）
PyYAML==6.0.2
