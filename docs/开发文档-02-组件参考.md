# 开发文档 02 · 组件参考（SDK / bridge / modules / common / 协议）

> 2026-08-16 重建版，基于实物编写。

## 1. wechat-agent-sdk 0.2.1（venv：wechat-claw/.venv/）

- `transport.py` — WeChatTransport（消息收发/媒体/轮询）；`api/client.py` — SessionExpiredError（会话过期，抛前清 token）
- `acp/adapter.py` — AcpAgent（SDK 的 on_start 引用不存在的 PermissionOutcome → bridge/session.py 的 ConfirmAcpAgent 重写绕过）
- `media/cdn.py + crypto.py` — CDN 加解密
- **本地补丁四处**（pip 升级会覆盖 → 重打脚本 `patches/apply_patches.py`，幂等 + py_compile + 关键行断言校验）：
  - ① cdn.py：`aes_key_b64 = base64.b64encode(key.hex().encode()).decode()`（格式 B，微信端可解密）
  - ② transport.py：`_build_media_item(..., file_size: int = 0)` + file 分支加 `len` 字段
  - ③ cdn.py：download URL 用 `quote(encrypt_query_param, safe='')` + `/download` 路径
  - ④ cdn.py：`from urllib.parse import quote`（③ 的符号依赖，保证补丁后文件可运行）
- **私有 API 使用点**（SDK 升级兼容检查项）：`main.py` 用 `transport._storage.load_token("default")` / `transport._client.token`（登录 token 恢复）、`session.py` 用 `agent._sessions.pop(...)`（会话归档）——以下划线开头成员升级可能改名/删除，升级前先验证存在性

## 2. bridge/（四大块，基础设施）

| 文件 | 职责 |
|---|---|
| `main.py` | 入口编排：transport/agent/会话组装、消息循环、push_worker/retry_worker；S1 捕获（SessionExpiredError → SystemExit(1)） |
| `push_server.py` | /push HTTP 入口：token 验算（index 哈希 + push_token）→ 校验 body（type ∈ 六类、≤100MB）→ 入队（有界 100，满 503）；401 记日志 |
| `scheduler.py` | 通用调度引擎：build_index() → 三态规则判定器（cron_match / every_interval / window 偏移严格版）→ spawn 模块（300s 超时）→ rc 感知 → retry 补发 |
| `session.py` | SessionManager（5h 窗口，归档移除会话键）/ PermissionGate（微信确认）/ ConfirmAcpAgent（SDK bug 修复） |
| `state.py` | 纯 IO：token 生成（chmod 600）/ retry 队列 / 调度状态 / 会话目标读取 |

## 3. modules/（业务模块，自描述）

| 模块 | module.json 调度 | retry | 入口参数 |
|---|---|---|---|
| todo | `every 1m` | 60s×0 | `--dry-run` |
| planner | `cron 29 8`（morning）+ `cron 0 21`（evening） | 900s×3 | `--phase morning/evening` `--dry-run` |
| emotion | `window 8-21 + random-5-50` | null（不补发） | `--dry-run` |

- `register.py`：token 重置（0600）+ module.json 字段保留式更新
- `registry_index.py`：build_index() 实时扫描 module.json + token 文件 → 汇总 index（scheduler/push_server 数据源）

## 4. common/（数据公共库）

| 文件 | 职责 |
|---|---|
| `task.py` | ParsedTask + 正则解析（vault 待办） |
| `config.py` | **基础设施配置层**：读 `~/.config/wechat-claw/config.yaml`（点号路径 `get("push.port")`，无文件=内置默认，部分配置深合并生效） |
| `io.py` | 防重/状态读写 + shared_load/shared_save + prune_state_file（修剪天数可配，默认 30） |
| `weather.py` | Open-Meteo/花粉（`nmgcdc.qcurl.cn` 官方域名）+ get_weather 缓存（TTL 30 分钟，shared 落盘） |
| `locations.py` | LOCATIONS（集宁默认 / 小店区备用） |
| `calendar.py` | lunar-python：农历/节气/三伏（**预留未接线**，无业务消费者） |
| `holidays.py` | 法定节假日（**预留未接线**，2026 数据待补） |
| `crypto.py` | AES-GCM 加解密（密钥路径可配，默认 `~/.config/wechat-claw/secret.key`，chmod 600 + deny）（**预留未接线**） |
| `push.py` | load_token / post_push（重试参数可配，默认 3×3s×15s；URL 由 push.host/port 合成） |
| `log.py` | log_event()（模块事件 JSON Lines；轮转参数可配，默认 1MB×2；bridge 标准 logging 写同文件，格式不同） |

**配置生效说明**：运行期启动时读取一次（模块级常量），改配置需重启 bridge / 重跑 worker 进程。

## 5. /push 协议

```
POST http://127.0.0.1:9898/push
Headers: Authorization: Bearer <token>（或 X-Token）
Body: {"type": <type>, "text": "...", "path": "..."}

type 分类：
  direct（原文直发）：briefing / notification / text / file（file 发文件原件，path 必带）
  agent（加工后发）：reminder / alert

响应：200 入队 / 400 非法 body / 401 鉴权失败 / 404 路径错 / 413 超 100MB / 503 队列满
```
