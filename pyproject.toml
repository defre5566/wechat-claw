[project]
name = "wechat-claw"
version = "0.0.1"
description = "微信主动推送体系：iLink 直连 + opencode 对话 agent（微信可主动关心、可干活）"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

# 依赖不在此声明：统一走 requirements.txt，防止双源维护。
# 本仓库不提供 pip 安装（部署 = venv + 源码运行 bridge.main）；无 build-system。